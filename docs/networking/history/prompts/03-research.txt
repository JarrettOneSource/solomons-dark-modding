Read /tmp/sd-netcode/brainstorm.md for context on what we're building (x86 DLL loader for a 2D top-down ARPG, co-op 4-player scale, native C++ with an embedded Lua runtime, Steam-distributed).

Your job is research — find what is CURRENT in 2025-2026 for co-op ARPG netcode and tell us what we might be missing. Use live web search aggressively. Cite sources inline with URLs. Prefer primary sources (GDC talks, devlogs, official docs, postmortems) over aggregator blogs.

Return a prioritized briefing with concrete recommendations — not a URL dump. For each finding, answer "should this change our current plan, yes / no, and how?"

Research priorities:

1. Current co-op ARPG netcode in the field. Dig into postmortems, GDC 2024/2025 talks, devlogs, engineering blogs for: Diablo 4, Path of Exile 2, Last Epoch, Grim Dawn, V Rising, Enshrouded, Torchlight Infinite, No Rest for the Wicked, Palworld, Lost Ark (since they did a migration), any 2024-2026 entries you can find. What authority model, what transport, what tick rate, what pain points have been publicly reported?

2. Steam transport landscape as of 2026. Is raw Steam P2P (`ISteamNetworking`) still preferred, or has `SteamNetworkingSockets` / Steam Datagram Relay (SDR) / `GameNetworkingSockets` become the default? Any deprecations announced? What are the tradeoffs today vs 2-3 years ago? Is SDR usable for non-retail mod-framework scenarios?

3. Non-Steam transports for dedicated servers. ENet, standalone GameNetworkingSockets, raw UDP, QUIC-based libraries (quinn, msquic, picoquic), WebTransport. For small-scale 4-player co-op on a 32-bit process, what is the pragmatic 2026 choice? Any new entrants (e.g. from Epic / Godot / Unity ecosystems) worth considering?

4. Replication paradigm. Has there been a shift in consensus for ARPGs between snapshot interpolation vs deterministic lockstep vs rollback netcode? Are there libraries or patterns that have become standard recently (netcode.io, Mirror, FishNet, Photon, Epic Online Services Transport, Nakama)? Anything adoptable into a native C++ x86 loader DLL?

5. Cheat mitigation for host-authoritative P2P where host is also a player. What has actually worked recently? Anti-cheat integrations worth considering (VAC, EAC, BattleEye, Ricochet, Wellbia) or skip entirely for a small mod framework that already expects modding? How do games like V Rising / Valheim handle trusted-host abuse in practice?

6. Dedicated + P2P hybrid architecture references. Valheim, Enshrouded, V Rising, Terraria, Palworld — how is their dedicated server actually implemented? Is it a headless copy of the game exe, a rewritten server binary, or an extracted sim loop? What's the transport abstraction they actually share between dedicated and P2P?

7. Matchmaking / lobby / session in 2026. Steam lobbies, Epic Online Services, direct IP, master server lists. What is the current best practice for a Steam-native co-op game that also wants dedicated? Any new SDKs (Playfab, Nakama, GameLift Anywhere, Edgegap) worth considering?

8. Surprise me. Anything 2025-2026 we might not know about that would materially change this design — new protocols (HTTP/3 for games?), hardware (programmable NICs?), academic results on snapshot interpolation for curved movers, anti-cheat advances, WASM netcode plugins, anything else.

Format: executive summary (5 lines), then findings ordered by relevance to our plan. For each finding: source citations, "how this affects our plan", and either "keep current decision" or "change decision to X because Y". End with a concrete recommendation set: pick transport X, pick replication paradigm Y, pick matchmaking Z, reason for each.