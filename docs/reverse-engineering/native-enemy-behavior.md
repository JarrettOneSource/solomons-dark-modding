# Native enemy behavior interpreter

## Scope and executable

This document is the gameplay-behavior contract for the browser rebuild. It
explains how a serialized `MonsterRecipe` becomes a live enemy brain: target
selection, movement, action scheduling, hit timing, damage dispatch, special
mechanics, and death. Sprite selection and animation presentation belong to
the sibling animation reconstruction. Death drops belong to the loot
reconstruction. Wave membership and scaling composition remain the authority
of [`data/wave.txt` and the wave scaling reconstruction](../wave-scaling-re.md);
they are cited here, not reconstructed again.

Addresses are for the supported retail `SolomonDark.exe`, SHA-256
`03a834566ce70fd8088f4cf9ee6693157130d8aec28c092cb814d6221231f1e3`.
The finite enemy census is generated by
`tools/build_native_enemy_catalog.py` and is cross-checked against
[`native-enemies.md`](native-enemies.md). A type is an enemy when the runtime
marks/tracks it as an enemy; multiplayer code must not replace that fact with
a type-ID whitelist.

## Construction pipeline

```text
MonsterRecipe::Sync 0x0063E890
  -> MonsterSetup_Parse 0x004AFBC0
  -> BuildEnemyConfig 0x0046B390
       applies arena HP/damage/chase/attack/XP scalars +0x9008..+0x9024
  -> factory/configure 0x005B7080 / 0x00463B50 / 0x00462790
  -> place and register 0x00469580
  -> target refresh 0x00483480
  -> common chase 0x004835F0 or a family-specific brain
  -> family action event
  -> contact-context setup 0x006246F0
  -> contact dispatch 0x0063E7D0
  -> target virtual +0x4C
```

Recipes are definitions, not actors. `0x00462790` copies the evaluated config
into family-specific live fields. Arena scalars are applied before that copy:
`+0x9008` multiplies HP; `+0x900C..+0x9018` multiply the four numeric damage or
duration lanes; `+0x901C` multiplies chase speed; `+0x9020` multiplies attack
speed; and `+0x9024` modifies XP. The browser interpreter should therefore
construct an immutable evaluated config, then instantiate mutable brain state.

## All 42 `MonsterRecipe` fields at runtime

The order and offsets below are the exact serializer ABI already decoded in
[`boneyard-scripting.md`](boneyard-scripting.md). `runtime` means the field has
a recovered actor/brain consumer. `definition-only` means it participates in
definition lookup or persistence but no per-tick brain consumer was found.
`reward-only` delegates the death result to the loot reconstruction. An
explicit `not-yet-reversed` verdict is deliberately different from zero or
unused.

<!-- MONSTER_RECIPE_FIELD_SEMANTICS_BEGIN -->
| Order | Offset | Field | Verdict | Runtime meaning | Native evidence |
| ---: | ---: | --- | --- | --- | --- |
| 1 | `+0x4C` | compiled enemy type | runtime | Selects the factory class and therefore the entire family state machine. It is not a behavioral enum within one universal brain. | `0x0046B390`, `0x00463B50`, `0x005B7080` |
| 2 | `+0x14` | recipe name | definition-only | Human/editor name retained with the recipe. No actor field or branch reads it after construction. | serializer `0x0063E890` |
| 3 | `+0x50` | recipe UID | definition-only | Stable script/group identity used to request this definition; it does not become an AI state. | relink `0x0064BC40`, spawn `0x00469580` |
| 4 | `+0x58` | maximum health | runtime | Multiplied by Arena `+0x9008`, then initializes live maximum/current HP at actor `+0x170/+0x174`; reaching the family death predicate enters death. | `0x0046B390`, `0x00462790` |
| 5 | `+0x5C` | primary damage | runtime | Multiplied by Arena `+0x900C`; copied to the family's primary hit lane (`+0x1B4` for Skeleton/Archer and most contact families, `+0x1B8` for Mage/Faculty primary). The action event stages it into the contact ABI. | `0x00462790`, Skeleton hit `0x00477580` |
| 6 | `+0x6C` | chase speed | runtime | Multiplied by Arena `+0x901C`, copied to live movement speed `+0x70`, and consumed by common or family movement. Constructor variance can further change per-instance movement. | `0x00462790`, chase `0x004835F0` |
| 7 | `+0x74` | movement/scale factor | runtime | Multiplies the actor's base world scale at `+0x30`; this changes world/collision size as well as presentation and therefore is gameplay state. It is not the chase-speed lane. | `0x00462790` |
| 8 | `+0x78` | variant/body mode | runtime | Family union: Skeleton/Archer weapon, Mage element, Zombie body. It selects attack dispatch or body-dependent behavior, not merely art. | `0x00462790`; family ticks below |
| 9 | `+0x7C` | projectile/weapon mode | runtime | Family union proven for Archer arrow payload (normal/fire/poison); other families with no recovered consumer ignore the evaluated value. | Archer fire `0x00477B90` |
| 10 | `+0xBC` | aura/effect mode | runtime | Archer accuracy/aiming mode in the recovered apply path; controls leading/scatter/random aim independently of arrow count. No universal aura behavior exists. | `0x00462790`, `0x00477B90` |
| 11 | `+0x80` | headgear/type mode | runtime | Skeleton-family headgear; family-discriminated union elsewhere. Skeleton headgear also receives wave-flag HP adjustments before actor creation. | `0x0046B390`, `0x00462790` |
| 12 | `+0x81` | type-specific union byte | runtime | Mage cloak selector. No gameplay-brain consumer is proven for non-Mage recipes. | `0x00462790` |
| 13 | `+0x82` | type-specific union byte | not-yet-reversed | Serialized and editor-visible, but the recovered `0x00462790` family apply paths do not copy it as an independent brain field. Preserve it in definitions; do not invent behavior. | serializer `0x0063E890`; negative apply-path result |
| 14 | `+0x83` | random-variant/armor mode | runtime | Skeleton armor/random-armor choice. The evaluated flag drives durability/combat construction and the live armor bytes rather than only sprite selection. | `0x0046B390`, `0x00462790` |
| 15 | `+0x30` | archetype/source name | definition-only | Names the source archetype/config from which the evaluated setup is built. Once type and config bytes are materialized, the brain does not compare this string. | parse `0x004AFBC0`, build `0x0046B390` |
| 16 | `+0xC1` | linked definition enabled | definition-only | Gates the serialized linked-definition relationship. No direct actor-brain read was found; preserve it for relinking. | serializer `0x0063E890`, relink `0x0064BC40` |
| 17 | `+0xC4` | linked UID | definition-only | Resolves to recipe pointer `+0xC8` after graph construction. No independent live-AI field is copied. | relink `0x0064BC40` |
| 18 | `+0x84` | behavior/count selector | runtime | Family union: Archer extra arrows; Mage self-shield HP; Imp/Demon split count; Zombie body; Faculty primary; Heart Crow count; Coffin max Maggots; Portal frequency. | apply `0x00462790`; family table below |
| 19 | `+0x88` | behavior minimum | runtime | Family union: Archer multi-arrow mode; Mage ally-shield HP; Faculty secondary; Heart blind enum; Coffin Maggot HP. It is not a universal numeric minimum despite the editor label. | `0x00462790`; family table below |
| 20 | `+0x8C` | behavior maximum | runtime | Family union: Mage shield interval; Faculty hat; Heart summon interval enum; Coffin Maggot damage. | `0x00462790`; family table below |
| 21 | `+0xB8` | flanking | runtime | Enables the common flanking/path choice used while approaching a valid target. | build `0x0046B390`, chase `0x004835F0` |
| 22 | `+0xB9` | pathfinding mode | runtime | Selects common pathfinding behavior and refresh cadence: absent definition `3`, mode 0 `10`, mode 1 `3`, mode 3 fixed `10` native ticks. | refresh `0x00483480` |
| 23 | `+0xCC` | orb-drop policy | reward-only | One independent eligibility roll feeding the one-candidate death selector. Drop content is owned by G7. | `0x0047C070`; [`native-enemies.md`](native-enemies.md#rewards-and-ground-drop-selection) |
| 24 | `+0xCD` | power-up-drop policy | reward-only | Power-up eligibility at death; no living-state transition. | `0x0047C070` |
| 25 | `+0xCE` | item-drop policy | reward-only | Generic item eligibility at death; no living-state transition. | `0x0047C070` |
| 26 | `+0xD0` | specific-item-drop policy | reward-only | Specific-item eligibility at death; no living-state transition. | `0x0047C070` |
| 27 | `+0xCF` | gold-drop policy | reward-only | Gold eligibility and its special value-5 lane at death; no living-state transition. | `0x0047C070` |
| 28 | `+0xD1` | potion-drop policy | reward-only | Potion eligibility at death; no living-state transition. | `0x0047C070` |
| 29 | `+0x54` | special spawn/type mode | runtime | Nonzero marks the created actor with the special bit `0x40`; values other than 3 can register it in the Arena's distinguished special-actor slot when that slot is empty. It changes lifecycle bookkeeping, not the factory type. | build/spawn `0x0046B390`, `0x00469580` |
| 30 | `+0x70` | attack speed | runtime | Multiplied by Arena `+0x9020`, copied to live `+0x17C`, and multiplies action-frame progress. Larger values shorten windup and recovery. | `0x00462790`; action ticks `0x0044BC20`, `0x00451A10`, `0x0044C400`, `0x0044D4F0` |
| 31 | `+0xD4` | XP modifier | reward-only | Multiplied/modified by Arena `+0x9024` and consumed by death reward accounting. It does not alter the living brain. | `0x0046B390`, `0x004819D0` |
| 32 | `+0x60` | secondary damage | runtime | Family union: Mage/Faculty secondary hit, Zombie poison-punch DPS, DemonSkull eye damage, Spider Cocoon HP. Multiplied by Arena `+0x9010`. | `0x00462790`; family table below |
| 33 | `+0x94` | shield/type flag | runtime | Mage self-shield toggle; Zombie flyblown flag; DemonSkull eye-laser capability; Heart/Spider family flag at their discriminated apply sites. | `0x00462790` |
| 34 | `+0x95` | shield-others/type flag | runtime | Mage ally-shield toggle; DemonSkull mouth-beam capability; Archer strafing. | `0x00462790` |
| 35 | `+0x96` | type-specific combat flag | runtime | DemonSkull Spit Fire capability. No universal meaning and no proven consumer in other family brains. | `0x00462790`, scheduler `0x00474930` |
| 36 | `+0x97` | burning/type flag | runtime | Skeleton-family/Wraith burning effect; DemonSkull Spit Imps payload bit. It is family-discriminated and must not be interpreted globally as cosmetic fire. | `0x00462790` |
| 37 | `+0x64` | tertiary damage | runtime | Family union: Zombie poison-pool DPS, DemonSkull beam DPS, Spider suck DPS. Multiplied by Arena `+0x9014`. | `0x00462790`; family event paths below |
| 38 | `+0x68` | extra damage/duration | runtime | Family union: Zombie poison duration, DemonSkull fire DPS, and other family extra payloads. Multiplied by Arena `+0x9018` before the live copy. | `0x00462790` |
| 39 | `+0x90` | behavior timer | runtime | Coffin Maggot poison damage is the proven family consumer. No universal timer semantics are justified for other types. | `0x00462790`, Coffin spawn `0x00479C30` |
| 40 | `+0x98` | first rectangle | not-yet-reversed | Serialized color/range rectangle. No independent copy into the recovered actor-brain layouts was found. Preserve it for definition/presentation parity; do not use it as an AI range. | serializer `0x0063E890`; negative apply-path result |
| 41 | `+0xA8` | second rectangle | not-yet-reversed | Serialized color/range rectangle. No independent copy into the recovered actor-brain layouts was found. Preserve it without assigning gameplay semantics. | serializer `0x0063E890`; negative apply-path result |
| 42 | `+0xC0` | casting/type mode | runtime | Archer range/casting mode and Mage range mode in the family apply branches; it changes range-control decisions and Mage cast setup. | `0x00462790`, Archer `0x00485200`, Mage `0x00490860` |
<!-- MONSTER_RECIPE_FIELD_SEMANTICS_END -->

The only declared `not-yet-reversed` rows are 13, 40, and 41. They are not
license to default values: round-trip their bytes exactly so future evidence
can assign semantics without a format change.

## Target acquisition and switching

This section extends, and does not repeat, the landed
[`native-enemy-target-acquisition.md`](native-enemy-target-acquisition.md)
work.

- `MonsterPathfinding_SelectNearestTarget (0x00481A60)` scans the stock
  gameplay hostile-candidate list, rejects native-ineligible `+0x160` actors
  and invalid ActorWorld buckets, and chooses minimum Euclidean distance.
  There is no player-vs-bot weight: a living gameplay-slot human or bot is one
  geometric candidate. Slot 0 is appended explicitly by the loader because it
  lacks a `ParticipantEntityBinding`.
- `MonsterPathfinding_RefreshTarget (0x00483480)` derives the cadence from
  recipe field `+0xB9`, selects/reloads the target, faces it, and writes phase
  `hostile+0x1DC = tick % hostile+0x1E0`. The recovered cadence is 3 ticks for
  no recipe, 10 for mode 0, 3 for mode 1, and 10 for mode 3.
- `Badguy_CommonChaseTick (0x004835F0)` adds periodic, missing-bucket, and
  25-tick refresh lanes. Family brains may refresh less often; therefore the
  authoritative loader re-evaluates every tracked live scene enemy at most
  once per 100 ms. A newly nearer candidate can switch on that bounded edge.
- On target loss, the native refresh path tries selection, reloads the exact
  bucket identity, and either resumes approach or chooses a random heading.
  The loader also reacquires after ActorWorld unregister and repairs the
  native `0x00483895` post-selection clear. A dead slot-0 target observes the
  documented death-transition grace before mutation.
- Host authority alone chooses. Clients resolve and apply the host-authored
  participant ID, owner-plus-native-type identity, or exact native bucket.
  Manual freeze, Turn Undead, explicit Lua targets, and client snapshots have
  higher priority than default nearest selection.
- GoodImp reverses allegiance and chooses tracked hostiles. Golem is already a
  native candidate; GoodImp and Leviathan are explicit validated sidecars.
  This does not admit arbitrary world actors.

Live bot consequences are recorded in
[`beta31-hostile-targeting-class-2026-08-04.md`](../bugs/beta31-hostile-targeting-class-2026-08-04.md)
and
[`beta32-bot-mana-and-lone-target-handoff-2026-08-04.md`](../bugs/beta32-bot-mana-and-lone-target-handoff-2026-08-04.md):
gameplay-slot actors are targetable, a targetless hostile can acquire a lone
bot on the maintenance edge, and a hostile with a still-valid human target
switches only when the nearest-selection edge runs.

### Common steering, flanking, and stalled-motion recovery

The shared path is not an A* or waypoint solver, but it is also not a normalized
straight-line chase. Fresh instruction review and an isolated live generated
Arena on 2026-08-22 close the state omitted by that simplification:

- `MonsterRecipe::MonsterRecipe 0x006400C0` writes the packed default word
  `0x0101` at recipe `+0xB8`: ordinary default monsters therefore start with
  `flanking=1` and `pathfindingMode=1`. Serialized custom recipes may replace
  either byte.
- `Badguy::Badguy 0x00473390` initializes the target-repath base at
  `actor+0x1E0=100`, the flanking timer at `+0x194=0`, speed and turn factors
  `+0x1A4/+0x1A8=1`, a signed flank angle in `[-90,90]` at `+0x18C`, a flank
  radius in `[0,100]` at `+0x190`, and an independent random steering heading
  at `+0x198`.
- `MonsterPathfinding_RefreshTarget 0x00483480` transforms the 100-tick base
  to 1,000 for mode 0, 300 for mode 1 or a null definition, leaves mode 2 at
  100, and writes 10 for mode 3. It stores the phase-locked remainder at
  `+0x1DC`; these are the live actor values, not the earlier shorthand
  `10/3/3/10` multipliers.
- `Badguy_BuildChaseVector 0x004763E0` turns the actor toward its selected
  steering point before producing movement. Each direction build changes
  heading by at most `baseTurn * turnFactor * statusFactor`, where base turn is
  constructor range `[0.5,1]`. During a flank it steers toward the target plus
  the stored angle/radius offset and decrements `+0x194`; otherwise it uses the
  ordinary target/wander branch. Movement remains
  `normalize(heading) * 0.25 * localSpeed * baseSpeed * statusFactor` per
  represented tick.
- `Badguy_SelectFlank 0x00473750` always replaces `+0x198` with
  `RandomFloat(360)`. When `+0x1F8 < 1` and the definition is null or enables
  flanking, it sets `+0x194 = 200 + RandomInt(201)`, `+0x1A4 = 1.5`, and
  `+0x1A8 = float32(1.4)`. Once the timer is inactive, both factors decay by
  exact double `0.995` toward a floor of one.
- `Badguy_CommonChaseTick 0x004835F0` calls the flank selector on its global
  20-tick / `RandomInt(150)==23` lane. Failed displacement increments
  `+0x1A0`; at 25 stalled eligible updates, fourteen of fifteen draws reroll
  the flank and clear the counter, while draw 3 invokes the class state-0D
  virtual. The winning branch does not clear `+0x1A0`. The shared virtual at
  `0x004737F0` queues action `0x0D`; its constructor `0x00448A20` consumes
  `RandomInt(50)`, stores a status-scaled 50..99-tick duration, and its tick
  `0x00449200` faces the live target while ordinary locomotion is suspended.
  Successful movement decrements the stalled counter instead.
- The normal movement cadence is two native ticks. The common tick builds and
  sums two direction vectors, adds the target-owned force vector from
  `0x0047CB20`, then submits the result through `Badguy_MoveStep 0x00475FE0`
  and `PlayerActor_MoveStep 0x00525800`. Large submissions are subdivided at
  `collisionRadius - 1`.

Injected-loader supporting evidence used retail executable SHA-256
`03a834566ce70fd8088f4cf9ee6693157130d8aec28c092cb814d6221231f1e3`,
PID 21192, generated Arena SHA-256
`2d836901476f6943212ba10e6f7680aabbc82d02676b44d12b0653d79897a929`,
and only `sample.lua.ui_sandbox_lab`. Every opening Skeleton exposed live
`+0x1E0=300`. A narrow trace of `0x00473750` recorded four calls in three
seconds; simultaneous live flank states included timers `232`, `245`, and
`294`, speed factor `1.5`, and turn factor `1.3999999761581`. The traces were
removed before the owned process was stopped. This runtime evidence supports,
but does not replace, the instruction-derived contract above.

## Skeleton family: executable template

The interpreter has two layers. The locomotion brain chooses conceptual
states; attacks live in the actor's embedded action manager at `+0xDC`.
`actor+0xE4` is the queued-action count and `actor+0xF0` points at the list.
Each list entry points to a shared-pointer control block whose first word is
the action object. The action ID is `action+0x14` and fractional progress is
`action+0x30`. These are enemy-action offsets, not the player's control-brain
offset `+0x21C`. A consumer must reject an ambiguous multi-entry queue instead
of silently selecting one; all accepted behavior traces observed exactly one
entry while an attack was active.

### Required transition relation

The following is the browser state relation. `attack_active` is an event edge,
not an animation pose. An action can re-enter it when another native marker
fires; presentation may display several surrounding frames, but only the
listed native callbacks apply gameplay output.

<!-- SKELETON_STATE_TRANSITIONS_BEGIN -->
| Archetype | From | Trigger | To | Native owner |
| --- | --- | --- | --- | --- |
| skeleton | spawn | actor registered and alive | search | `0x004771B0`, `0x00484B90` |
| skeleton | search | valid target acquired | approach | `0x00483480`, `0x00484B90` |
| skeleton | approach | target lost or bucket invalid | search | `0x004835F0` |
| skeleton | approach | target in weapon reach and scheduler ready | attack_windup | `0x00473960`, actions `0x0E..0x10` |
| skeleton | attack_windup | first native action marker fires | attack_active | `0x0044BC20`, `0x00451A10`, `0x0044C400` |
| skeleton | attack_active | marker callback returns and action remains queued | attack_recovery | `0x00477580` |
| skeleton | attack_recovery | later native action marker fires before completion | attack_active | `0x0044BC20`, `0x00477580` |
| skeleton | attack_recovery | action progress exceeds end frame | approach | action tick for `0x0E..0x10` |
| skeleton_archer | spawn | actor registered and alive | search | `0x0048A6B0`, `0x00485200` |
| skeleton_archer | search | valid target acquired | range_control | `0x00483480`, `0x00485200` |
| skeleton_archer | range_control | target lost or bucket invalid | search | `0x00485200` |
| skeleton_archer | range_control | range/cooldown predicate accepts | attack_windup | `0x00473B40`, action `0x11` |
| skeleton_archer | attack_windup | progress crosses frame 13 | attack_active | `0x0044D4F0` |
| skeleton_archer | attack_active | Arrow objects registered | attack_recovery | `0x00477B90` |
| skeleton_archer | attack_recovery | progress exceeds frame 16 | range_control | `0x0044D4F0` |
| skeleton_mage | spawn | actor registered and alive | search | `0x0048ABB0`, `0x00490860` |
| skeleton_mage | search | valid target acquired | range_control | `0x00483480`, `0x00490860` |
| skeleton_mage | range_control | target lost or bucket invalid | search | `0x00490860` |
| skeleton_mage | range_control | cast predicate accepts | attack_windup | `0x00478290`, action `0x12` |
| skeleton_mage | attack_windup | progress crosses selected frame 25 or 31 | attack_active | common cast action tick |
| skeleton_mage | attack_active | element callback/shield effect dispatched | attack_recovery | `0x0047FDE0` |
| skeleton_mage | attack_recovery | a later configured action marker fires | attack_active | common cast action tick |
| skeleton_mage | attack_recovery | progress exceeds paired frame 41 or 47 | range_control | common cast action tick |
| skeleton_mage | range_control | shield interval expires and a configured recipient is valid | shield_special | `0x00490860` |
| skeleton_mage | shield_special | shield action completes | range_control | actions `0x13`, `0x14` |
| skeleton_family | any_alive | HP/death predicate fires | death | common `0x004819D0`, family overrides |
<!-- SKELETON_STATE_TRANSITIONS_END -->

Skeleton movement is `0x004773E0`, tick/schedule is `0x00484B90`, action
dispatch is `0x00473960`, and hit is `0x00477580`. Weapon mode chooses Claw
`0x0E`, ordinary weapon `0x0F`, or Pike `0x10`; sword, mace, flail, and axe
share the ordinary weapon timing but retain their evaluated damage/durability
changes. Armor/headgear do not add a new AI state. Death presentation differs
by equipment but the gameplay terminal state and reward entry are the same;
see [`skeleton-death-effects-re.md`](../skeleton-death-effects-re.md).

Archer movement/range control is `0x00477B40`/`0x00485200`, scheduling is
`0x00473B40`, and its active callback `0x00477B90` creates `Arrow 0x7DA`.
The arrow receives source/team, origin, heading, primary damage, and the
normal/fire/poison payload. Accuracy mode, extra-arrow count, multi-arrow mode,
and strafing are independent. Scatter directions use an actor-scoped RNG
seeded from actor `+0x1C0`; do not substitute browser `Math.random`.

The 2026-08-27 residual drain closes every previously bounded Archer targeting
formula. `SkeletonArcher` construction writes
`attackRange = 280 + Float(170)`. MonsterRecipe range modes apply exact factors
`[1, 1/1.8, 1.5, 1/1.8]`; mode 3 also arms byte `+0x251`, so the first action
callback restores the shortened range by multiplying by `1.8` once and clears
the constructor latch at `+0x250`. There is no minimum range or retreat band.
Shot admission is strict distance `< attackRange`, the presentation-ready byte,
and line of sight.

`Action_Skeleton_ShootArrow::Tick 0x0044D4F0` recomputes `actor+0x6C` from the
current target position on every action tick. The accepted moving-player golden
matches that direct heading within float noise at every sampled tick; freezing
the heading at windup entry is not stock behavior. `Action_Skeleton_ThrowSpell`
uses the same tick function, so Skeleton Mage casts share this tracking owner.
The Skeleton claw/weapon/pike action ticks `0x0044BC20`, `0x00451A10`, and
`0x0044C400`, plus Zombie Beat `0x00449300`, independently write the same live
target-facing lane during their actions.

That writer is action-specific, not a universal rule. `Demon::Tick
0x00487300` returns from its active-action branch before common chase
`0x004835F0`, and `Action_Demon_Spit::Tick 0x0044DF00` advances progress and
writes controller selector `actor+0x2DC` without touching heading `+0x6C`.
The Lesser Demon therefore holds both position and action-entry facing through
the bomb action. By contrast, Wraith `0x00486C30` calls common chase
unconditionally before its drain/contact logic, and hostile Imp `0x00485DC0`
does the same before flight/contact processing.

Scheduling `0x00473B40` overwrites `actor+0x1C0` with shared
`Integer(1,000,000)`, not the formerly documented ten-million bound. At the
active callback, `0x00477C54` first requests registry entry 78
`sounds\\shootarrow` at point attenuation and pitch
`1 + SignedFloat(0.1)`. Accuracy mode 3 then consumes shared `Integer(3)` and becomes
mode 0, 1, or 2 for that volley. The callback then seeds a private native RNG
from `+0x1C0`:

- mode 0 aims at the current target;
- mode 1 adds `targetVelocity * (distance / 6)` to the current target point;
- mode 2 adds `Float(75) * headingVector(Float(360))` to the target point; and
- mode 3 has no separate angular distribution beyond its shared mode choice.

Multi-arrow modes map to stored chance thresholds `[0,15,50,100]`. When the
threshold is positive, private `Integer(100) <= threshold` enables the authored
extra-arrow count, so the nominal 15/50 lanes accept 16/51 integer results.
The fan sequence is exactly `0,-10,+10,-20,+20,...` degrees; each nonzero lane
draws between `0.9*offset` and `1.1*offset` from the private stream. Every Arrow
starts 30 units forward, draws speed `5.7 + Float(0.6)`, and stores
`round-even((distance + 100 + Float(100)) / speed)` as its lifetime. These
draws occur in arrow order and must not be replaced by a fixed speed, analytic
intercept, four-degree fan, or browser RNG.

There is one native presentation-to-gameplay latch that G4 must preserve at
the seam. Archer predicate `0x00477A80` rejects the shot until byte
`actor+0x248` is nonzero, a target exists, squared distance is less than
`(actor+0x1B0)^2`, and line-of-sight `0x00524D70` accepts. Render callback
`0x0048F450` raises `+0x248`; a truly headless retail run left it zero and the
Archer never queued `0x11`, while a visible run raised it to one and attacked.
The browser brain therefore needs an explicit presentation-ready input, not a
hidden dependency on whether a canvas happened to draw that frame.

Mage movement/range control is `0x00478380`/`0x00490860`, scheduling is
`0x00478290`, and active dispatch is `0x0047FDE0`. Element branches create
`Firebolt 0x7EB`, `GuidedMissile 0x7EC`, or run their recovered direct/status
contact paths. Self and ally shields have separate toggles/strengths and a
shared interval. Mage construction replaces the inherited Archer range with
`312 + Float(150)`; the four recipe range modes use the same exact factors
`[1, 1/1.8, 1.5, 1/1.8]`. As with Archer, this is one strict maximum range,
not an approach/retreat band. The per-cast rate variance consumes the active
shared native RNG stream at `0x00818B08`.

### Shared obstacle-route owner

The earlier movement pass stopped at `Badguy_BuildChaseVector` and
`PlayerActor_MoveStep`. The complete shared owner also includes vtable slot
`+0x74 = Badguy_ResolveNavGoal 0x00483D40`. When zero-width Region LOS to the
live target is blocked, it calls triangle-navmesh A* `0x005DFF20`, retains two
portal-derived waypoints, advances them on the native `dot <= 100` crossing
test, refreshes them on `actor+0x1E0`, and then submits the resulting steering
vector through the same movement/collision executor. Ordinary Badguys select
the clearance-25 mesh; Demon/DemonSkull write selector `+0x15D=1` and select
clearance 50. Full builder, A*, vtable membership, NPC xref disposition, and
teardown fields are recorded in
[`docs/pathfinding-investigation.md`](../pathfinding-investigation.md#2026-08-27-native-navmesh-and-enemy-targeting-closure).

### Attack timing and damage edge

At fixed actor delta 1, per-marker rate multiplier 1, and attack speed 1,
action progress increases by `base_rate * actor+0x17C` each native tick.
Completion is the first tick with progress strictly greater than the end
frame. The action code can reroll its marker-rate multiplier, so the first four
rows are nominal integer bounds for a multiplier of one, not a promise that a
live action always lasts that many wall ticks. The fixture preserves the
actual progress and event ticks.

<!-- ENEMY_ATTACK_TIMING_BEGIN -->
| Attack | Action | Base rate/tick | Active frame | End frame | Windup ticks | Total ticks | Recovery ticks |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Skeleton claw | `0x0E` | `0.125` | `4` | `7` | `32` | `57` | `25` |
| Skeleton weapon | `0x0F` | `0.25` | `9` | `24` | `36` | `97` | `61` |
| Skeleton pike | `0x10` | `0.125` | `2` | `12` | `16` | `97` | `81` |
| Skeleton Archer | `0x11` | `0.0843750015` | `13` | `16` | `155` | `190` | `35` |
| Skeleton Mage | `0x12` | `0.253125012 * (1 + roll)` | `25` or `31` | `41` or `47` | variable | variable | variable |
<!-- ENEMY_ATTACK_TIMING_END -->

`SkeletonAction_Tick 0x0044BC20` tests both native marker constants (`4.0` at
`0x007849F8` and `8.0` at `0x007849DC`) and invokes the family callback on a
crossing. The action can wrap at completion, so damage is not a single
presentation-frame Boolean. In the accepted stationary claw trace, the first
complete action starts at fixture tick 101, reaches the derived frame-4 marker
at 133, produces three separately observed `3.0` HP resolver events at ticks
133, 134, and 161, and leaves the queue at tick 162. Those are gameplay
events, not duplicated sprites; the static contract pins that exact witness.

The Skeleton active callback calls `0x006246F0`, stages damage/source in
`0x0081C6E0..0x0081C6F8`, and calls `0x0063E7D0(target)`. That dispatcher
checks same-world contact and invokes target virtual `+0x4C`; player damage
then proceeds through the landed authoritative player-damage hooks. This is
the same contact ABI documented in
[`native-projectile-and-spell-mechanics.md`](native-projectile-and-spell-mechanics.md#damage-application-path-and-accepted-goldens).
Projectile flight, collision queries, modifiers, and contact are owned by
[`native-projectiles-and-effects.md`](native-projectiles-and-effects.md) and
[`native-projectile-and-spell-mechanics.md`](native-projectile-and-spell-mechanics.md);
the enemy brain owns only when and with what payload the projectile is born.

## Remaining compiled archetypes

Every row includes the field-to-state connection, the minimum browser state
machine, gameplay outputs, RNG source, and confidence. `death` means the
family override (if any) followed by common reward handling; drop contents are
not repeated here. If a row omits windup, flee/leash, or a separate cooldown,
no distinct state was recovered for that family: use its stated contact or
timer edge and the common target-loss return to search rather than inventing
one.

| Type / confidence | State machine and transitions | Recipe-driven gameplay outputs | RNG stream and seed |
| --- | --- | --- | --- |
| `Badguy 0x3E8` / high | Abstract `spawn -> search -> approach -> contact/cooldown`; target loss returns to search; death is terminal. Refresh `0x00483480`, chase `0x004835F0`, contact `0x0048A290`, death `0x004819D0`. | HP, chase, attack speed, flanking/pathfinding, common damage/reward lanes. Concrete classes override the action edge. | Common no-target heading and construction variance use the active shared stream. |
| `Imp 0x3EC` / high | `spawn/airborne -> approach -> contact -> cooldown -> approach`; `any_alive -> death -> split`. Tick `0x00485DC0`, chase `0x00478560`, split `0x004824A0`. | Live `+0x210` is remaining split depth. A permitted death always creates exactly two child Imps and writes each child's `+0x210 = parent +0x210 - 1`; the depth is not a child count. Splitting requires depth at least one and live Imp count at most 68. | Placement/child variation uses the active shared stream; each actor's `+0x1C0` seed originates in a shared-stream construction draw. |
| `GoodImp 0x3ED` / high | `spawn -> acquire_hostile -> approach -> contact/cooldown`; lifetime expiry enters terminal ally release, not hostile reward death. Init `0x0052A050`, tick `0x0052C1A0`. | Inherited combat payload and ally lifetime; targets tracked enemies with reversed allegiance. | Active shared stream for construction/action variance. |
| `GreenImp 0x7FC` / high | Same flying approach/contact loop as hostile Imp; spawned in an ejected state by accepted Unholy Spit impacts; death terminal. | Inherits team and scaled primary/secondary damage from Unholy Spit, not a standalone MonsterRecipe in the ordinary wave path. | Parent projectile placement samples the active stream; child construction seeds its actor field from that stream. |
| `Zombie 0x3EE` / high | `spawn -> search -> approach -> attack 0x17 -> cooldown -> approach`; death emits optional pool then terminates. Tick `0x004863A0`, attack `0x0047DB90`, death `0x004947B0`. | `+0x84` body, `+0x94` flyblown, secondary/tertiary/extra lanes become poison punch DPS, pool DPS, and duration. Contact applies knockback and `Poisoned 0x1B72`; death can create `PoisonPool 0x806`. | Active shared stream for constructor/action variation. Damage itself has no universal variance roll. |
| `Wraith 0x3EF` / high | `spawn -> approach -> orbit/retreat -> attack -> cooldown(50) -> approach`; retreat/orbit timer `200 + RandomInt(601)`; fade/visibility is a special state; death terminal. Tick `0x00486C30`, movement `0x00478EA0`, death `0x00495600`. | Primary hit applies `Dazzle 0x1B6E`; burning owns the fire child. | Retreat timer and ordinary choices consume the active shared stream. |
| `DemonSkull 0x3F0` / high | `spawn -> approach/schedule -> bite 0x18 | eye 0x1A | beam 0x1B | spit 0x1C -> recovery -> schedule`; scream/flair are separate specials; death terminal. Schedule `0x00474930`, tick `0x004963C0`, event `0x00498180`. | `+0x94` eye creates two `EyeLaser 0x7FF`; `+0x95` beam scans geometry with tertiary DPS; `+0x96` creates `UnholySpit 0x7FB` with extra fire DPS; `+0x97` adds Imp spawning to that spit payload. | Scheduler uses active shared draws. SpitFire action `0x00449880` uses a private RNG seeded from actor `+0x1C0`; its seed originally came from shared construction. |
| `Demon 0x3F1` / high | `spawn -> approach/schedule -> bomb -> recovery`; optional fire special; `death -> radial_split -> terminal`. Tick `0x00487300`, event `0x0049A270`, death `0x00482930`. | Primary damage feeds `DemonBomb 0x7F7`; `+0x84` sets death Imp count. | Active shared stream for schedule and radial placement; children receive actor seeds from construction draws. |
| `DireFaculty 0x3F2` / high | `spawn -> search -> range_control (approach outside range, retreat/reposition inside it) -> primary_windup 0x1F/0x21 or secondary_windup 0x20 -> active -> recovery -> cooldown -> range_control`; target loss returns to search and death is terminal. Trigger `0x00475340`, tick `0x0049D0D0`, event `0x004804D0`. Live markers are frame 15 for `0x1F` and frame 20 for `0x20`. | `+0x84/+0x88` select primary/secondary; primary 0 SkullMissile `0x800`, 1 direct Blightning, 2 DarkFireball `0x804`; secondary 0 RainOfBones `0x801`, 1 TragicCircle `0x802`, 2 DarkFireball ring. Index 3 labels have no event branch. Scheduler writes cooldown 300 and rerolls `+0x1B0` range. | When live cooldown `+0x260 < 0`, `RandomInt(2)==1` selects secondary, then resets `+0x260` to `1500 + RandomInt(1001)`. Both draws and the post-schedule range float use the active shared RNG. |
| `Heartmonger 0x3F3` / high | `spawn -> orbit_control -> crow_attack and summon_scheduler -> cooldown`; death detaches surviving Crows then terminates. Init `0x00488F20`, tick `0x00489000`, death `0x0049FB60`. | `+0x84` allocates Crows; `+0x88` maps blind enum to 0/5/10/30/100%; `+0x8C` controls randomized Skeleton/Archer/Imp summon interval. Crow strike `0x0047A160` uses primary damage. | Orbit phases are deterministic by count; blind, summon interval/type/count use active shared RNG. |
| `Crow 0x3F4` / high | `parent_orbit -> scan -> dive -> strike -> return`; invalid parent causes detach/cleanup according to parent-death state. Tick `0x0047EA70`, strike `0x0047A160`. | Parent identity, orbit phase, and target identity are runtime child fields; not independent recipe rows. | Target/action timing and blind roll use Heartmonger/active shared draw order. |
| `Coffin 0x3F5` / high | Exact four-state `closed -> opening/charging -> transition_delay -> open/active`; open replenishes while under max; death terminates. Tick `0x004A2760`, spawn `0x00479C30`. | `+0x84` max Maggots, `+0x88` child HP, `+0x8C` child primary damage, `+0x90` child poison damage. Opening invokes the spawn helper three times. | Lid/edge trajectory and replenishment choices use active shared RNG. |
| `Maggot 0x7FD` / high | `ballistic_emerge -> crawl/approach -> single_bite -> self_death`; invalid Coffin parent cleans up. Ctor `0x0047E0F0`, emerge `0x0047E410`. | Copies Coffin child HP/damage/poison. A valid bite damages/poisons (except Golem) once, decrements ownership, and immediately dies. | Emergence trajectory uses active shared construction draws. |
| `Spider 0x809` / high | Tick `0x0047A580` uses raw states `0..5`: approach/range choice, hop/lunge, web windup, grab/hold, suck cadence, recovery; invalid target returns to search; death terminal. Hop `0x00475CD0`, web gate `0x00475DB0`. | `+0x94` enables web spit; secondary becomes Cocoon HP; tertiary becomes suck DPS. `0x00475AC0` creates `Silk 0x808`; grab deals primary once and suck deals half configured DPS every 25 ticks. | Hop/web choice and aim variation use active shared RNG. |
| `Cocoon 0x80A` / high | `attach/immobilize -> hold` while target/full-web identity is valid; release or target loss enters cleanup/death. Tick `0x00475D10`, release `0x0048BCE0`. | Target-owned duplicate-safe identity; HP payload comes from Silk/Webbed severity. It is containment behavior, not a drop-bearing enemy brain. | No behavior roll recovered in the hold/release machine. |
| `Portal 0x139D` / high | `materialize(10 ticks) -> idle_countdown -> spawn_imp -> reset_countdown`; contact effect does not move it; death terminal. Tick `0x00489CC0`, contact `0x0048C370`, death `0x004A1FA0`. | `+0x84` frequency maps to inclusive lower/upper bounds: 0=`8..10`, 1=`6..8`, 2=`3..4`, 3=`2..3`, 4=`1..2`, 5=`0.25..0.5`, scaled by `0x00820230`. Expiry spawns an ejected Imp with team/primary damage. | Initial `countdown += RandomInt(upper/3)` and ordinary inclusive reset use active shared RNG; a one-in-eight roll instead resets with `RandomInt(upper)`. |

The Faculty secondary witness closes a gameplay distinction that its name does
not reveal. `0x004804D0` copies live secondary lane `actor+0x1B4` into
`RainOfBones+0x154`; `RainOfBones_Tick 0x0061C440` stages that value into the
normal contact ABI. In both accepted traces its `0x801` contact set player HP
to exactly one percent of the pre-contact value: stationary
`4961.474609 -> 49.614746`, moving `4961.254883 -> 49.612549`. Treat this as
the stock `Acid Pain` gameplay result, not flat damage equal to the displayed
HP delta. The same traces observe ordinary `SkullMissile 0x800` hits for
`20.0` before that percentage contact.

No self-heal transition was recovered anywhere in the 19-type census. Mage
self/ally shield is absorption state, not HP restoration. The actual creation
mechanics are split-on-death for Imp/Demon, Heartmonger timed summons, Coffin
owned-Maggot replenishment, and Portal countdown spawns; the table above names
their owner, payload fields, and RNG source.

Death/special presentation addresses and fragment behavior are intentionally
cross-referenced, not duplicated. They are in
[`native-enemies.md`](native-enemies.md) and, for the skeleton family,
[`skeleton-death-effects-re.md`](../skeleton-death-effects-re.md).

## RNG contract for the browser

The primitive is the 55-word lagged-Fibonacci generator at `0x00401170`; float
draws use `0x00401310`. The active pointer is `0x00818B08`. Ordinary startup
seeds the shared global from `timeGetTime`; deterministic loader runs can seed
it through `sd.rng.set_seed`. Level/screen construction also has twelve
recovered seed sites using `App+0x28 * 0xEF3`, so elapsed unpaused app ticks are
part of native seed state. Full recurrence and float32 rounding are specified
in [`native-movement-and-tick.md`](native-movement-and-tick.md#native-rng).

Enemy construction normally consumes shared draws and places a derived seed
at actor `+0x1C0`. Most scheduling/wander decisions continue on the shared
stream. The recovered private exceptions relevant here are actor-seeded drop
selection and DemonSkull SpitFire; Archer scatter also scopes its aiming draws
to actor state. A browser rebuild must preserve both stream identity and draw
order. Removing a gameplay-adjacent presentation draw from the shared stream
without an equivalent advance changes later behavior.

The live recorder writes `RUN_SEED = 0x013579BD` to the shared stream before
`sd.hub.start_match`, but does **not** call that a complete run seed. Level
construction may reseed from `App+0x28 * 0xEF3` as documented by G1. Every
trace therefore records the spawned enemy's actual `actor+0x1C0` construction
seed and the first/last native tick. Replays key behavior rolls by the named
shared or actor-private stream, never by the requested seed alone.

## Live golden format

[`enemy-behavior-goldens.json`](../../tests/fixtures/webgame/enemy-behavior-goldens.json)
is recorded from a small owned retail instance by
`tools/record_enemy_behavior_goldens.py`. Each scenario records every native
tick's enemy position, conceptual/raw state fields, facing, action ID/progress,
target position, HP, attack events, projectile births, and observed damage
amounts. Skeleton, Archer, Mage, and Dire Faculty each run against both a
stationary local player and a deterministic moving local player driven by
`sd.input.hold_movement_frames`. The target moves in an 80-tick `+Y`, 120-tick
idle, 80-tick `-Y`, 120-tick idle cycle. The recorder primes the stock wave
spawner through `sd.gameplay.start_waves` and selects an exact stock group; it
never calls an enemy constructor directly. Faculty traces explicitly set only
its live secondary cooldown `+0x260` to `-1` so the native random scheduler can
exercise the special during the bounded window; that setup is recorded in
each trace.

Queue start/end events come from the native action manager, active markers are
labelled `derived_action_progress_marker`, projectile births come from native
world registration, and damage comes from the loader's before/after player
resolver observation. The committed fixture and standalone evidence copy are
byte-identical. Fixture SHA-256:
`f246c280e7fea7c90573eb6fcb2636adbc4e327f6f15b5caa12735bf0453a82e`.

The fixture is an acceptance oracle, not a replacement for the state tables:
one trace proves the executed branch and timing, while the static reconstruction
specifies the unexecuted config branches.

GoodImp's lifetime is now closed independently of recipe generalization. Its
constructor/default Ember path assigns 300 ticks. Each ordinary tick decrements
the lifetime once, and the targetless branch decrements it a second time. On
expiry it creates the Fire release presentation and removes the allied actor;
it does not enter the hostile reward-death path.

## Not Yet Reversed

- Recipe `+0x82`, `+0x98`, and `+0xA8` have no independent recovered live-brain
  copy. They must round-trip exactly but have no invented AI effect.
- The exact probability formulas for every family scheduler branch have not all
  been reduced to closed form. The table names the owning stream so a port can
  reproduce native draw order even where the human-readable probability is
  not yet simplified.
- Dire Faculty primary/secondary index 3 have editor labels but no executable
  event branch in `0x004804D0`; they are dormant, not missing browser work.
- Several boss recovery frame constants remain executable-trace/state-machine
  facts rather than generalized recipe fields. Preserve the native trace
  behavior; do not infer them from unrelated fields.
